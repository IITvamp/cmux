{
  "name": "@cmux/native-time",
  "version": "0.1.0",
  "private": true,
  "description": "Native Node-API addon (Rust) that returns current time",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "build": "bunx --bun @napi-rs/cli build --platform",
    "build:release": "bunx --bun @napi-rs/cli build --platform --release"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4"
  }
}

