{
  "name": "@cmux/edge-router-gcp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development tsx watch src/index.ts",
    "start": "NODE_ENV=production node dist/index.js",
    "build": "tsc",
    "docker:build": "docker build -t edge-router-gcp .",
    "docker:run": "docker run -p 8080:8080 edge-router-gcp",
    "docker:dev": "docker-compose up --build"
  },
  "dependencies": {
    "express": "^4.21.2",
    "node-html-parser": "^6.1.13",
    "undici": "^7.2.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
